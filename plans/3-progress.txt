2026-05-13

- Created initial Ralph planning scaffold for this repository.
- Current PRD focus is the Mole working-instance lifecycle and command surface.
- No Ralph PRD tasks have been completed yet.
- Existing source context used for the initial scaffold:
  - README.md
  - docs/vision/open-source-product-direction.md
  - plans/working-instance-upgrade-plan.md
  - docs/cli-and-command-surface.md
  - cli/README.md

2026-05-13

- Completed `instance-lifecycle-001`: expanded `mole doctor` to report source version, instance version, and missing metadata warnings.
- Added a Node test harness for the CLI and covered doctor output with and without `mole.instance.yaml`.
- Validation: `npm test` from `cli/` passed.
- Next priority: `instance-metadata-and-doctor` / `instance-lifecycle-002`.

2026-05-13

- Completed `instance-lifecycle-002`: added `upgrade-ownership.json` with safe-copy, merge-carefully, and never-overwrite classes.
- Documented the ownership classes in `docs/upgrade-ownership.md`.
- Added a validation test that parses the manifest and confirms `4-context/`, `5-evidence/`, and `6-raw/` are never-overwrite paths.
- Validation: `npm test` from `cli/` passed.
- Next priority: `upgrade-report` / `instance-lifecycle-003`.

2026-05-13

- Completed `instance-lifecycle-003`: replaced the placeholder `check-updates` command with a read-only report.
- The report compares source and instance versions, reports update status, and lists safe-copy paths separately from merge-carefully paths.
- Added tests for up-to-date and source-newer instance states.
- Validation: `npm test` from `cli/` passed.
- Category `instance-lifecycle` is complete. Next priority category: `command-surface`.

2026-05-13

- Completed `command-surface-001`: normalized CLI help and visible command text around `Mole` for the product name and `mole` for command examples.
- Added a help-output test covering the main command examples and preventing `Cascade` regressions in help text.
- Validation: `npm test` from `cli/` passed; `node cli/mole.mjs --help` produced the expected help text.
- Category `command-surface` is complete. No incomplete PRD tasks remain.

2026-05-13

- Completed `team-collaboration-001`: documented the shared synced-folder team operating model in the README.
- Added provider guidance for SharePoint/OneDrive, Google Drive for desktop, Dropbox desktop app, and Box Drive.
- Clarified that many contributors can capture raw inputs while one maintainer or agent should process and promote inbox material at a time.
- Validation: `jq empty plans/prd.json` passed; `npm test` from `cli/` passed.
- Next priority: `team-safe-capture` / `team-collaboration-002`.

2026-05-13

- Completed `team-collaboration-002`: added a shared capture filename helper used by both CLI capture and the local UI capture endpoint.
- Capture filenames now include a UTC timestamp, slug, and unique suffix, and writes refuse silent overwrite.
- Added tests for repeated similar notes and UI capture paths.
- Validation: `npm test` from `cli/` passed.
- Next priority: `team-safe-capture` / `team-collaboration-003`.

2026-05-13

- Completed `team-collaboration-003`: added `captured_by` metadata to CLI and UI raw captures.
- CLI capture defaults attribution from `MOLE_CAPTURED_BY`, then local username, then `unknown`; the UI now exposes a `Captured by` field.
- Added tests for attribution defaults and emitted frontmatter.
- Validation: `npm test` from `cli/` passed.
- Next priority: `inbox-processing-coordination` / `team-collaboration-004`.

2026-05-13

- Completed `team-collaboration-004`: documented shared inbox states for `new`, `processing`, `processed`, and `archive`.
- Updated agent guidance to treat legacy direct inbox folders as unprocessed input and never delete raw inputs before a retrieval receipt exists.
- Documented weak signal and substantive artefact movement through the state flow while preserving compatibility with current capture paths.
- Validation: `npm test` from `cli/` passed; state documentation checks passed with `rg`.
- Next priority: `inbox-processing-coordination` / `team-collaboration-005`.

2026-05-13

- Completed `team-collaboration-005`: added a file-based inbox processing lock and JSON receipt convention.
- Added `mole inbox claim [processor]` and `mole inbox complete [summary]` commands.
- Documented stale-lock handling and updated shared inbox agent guidance.
- Added tests for one active claim, safe concurrent-claim failure, receipt creation, and lock release.
- Validation: `npm test` from `cli/` passed; `node cli/mole.mjs --help` lists the inbox commands.
- Category `team-collaboration` is complete. No incomplete PRD tasks remain.

2026-06-11

- Completed `molehill-metrics-001`: added the processed inbox metrics data foundation under `governance/metrics`.
- Added `lib/metrics.mjs` with UTC daily counting, same-day path dedupe, starter file creation, ISO week rollups, calendar month rollups, and retention trimming.
- Added tests for unique counting, same-day dedupe, UTC date reset, daily retention, weekly/monthly retention, and preserving historical rollups.
- Validation: `npm test` from `cli/` passed.
- Next priority: `metrics-inbox-integration` / `molehill-metrics-002`.

2026-06-11

- Completed `molehill-metrics-002`: extended `mole inbox complete` with repeated `--processed <path>` support.
- Completion receipts now preserve processed paths and successful completions update local metrics after the receipt is written.
- Metrics update failures are reported as warnings and do not fail completion or prevent lock release.
- Validation: `npm test` from `cli/` passed.
- Next priority: `metrics-dashboard` / `molehill-metrics-003`.

2026-06-11

- Completed `molehill-metrics-003`: added the static Molehill Metrics dashboard at `governance/metrics/dashboard.html`.
- The dashboard reads daily, weekly, and monthly metrics JSON, supports view switching and date filtering, and displays totals, averages, latest values, a table, and a simple bar chart.
- Updated workspace scaffolding so new Mole workspaces include the dashboard and starter metrics files.
- Validation: `npm test` from `cli/` passed.
- Next priority: `metrics-guidance` / `molehill-metrics-004`.

2026-06-11

- Completed `molehill-metrics-004`: documented the processed-path metrics workflow in README, CLI docs, inbox synthesis skill guidance, and bootstrap agent rules.
- Updated `mole synthesise inbox` output so generated agent instructions include `mole inbox complete --processed <path> ... "summary"`.
- Validation: documentation checks with `rg` passed; `npm test` from `cli/` passed.
- Category `molehill-metrics` is complete. No incomplete PRD tasks remain in the category.

2026-06-11

- Completed `molehill-metrics-005`: added explicit historical metrics backfill with `mole metrics backfill`.
- Backfill reads `governance/run-receipts/inbox-processing/*.json`, counts only receipt `processed` paths with valid `completed_at` dates, dedupes per UTC day, and rebuilds daily, weekly, monthly, and current-day dedupe metrics.
- Documented backfill for upgraded existing workspaces and clarified that it does not infer from raw folders or read raw insight content.
- Validation: `npm test` from `cli/` passed; documentation checks with `rg` passed.
- Category `molehill-metrics` is complete. No incomplete PRD tasks remain in the category.

2026-09-07

- Completed `command-surface-002` for GitHub issue #17: implemented `mole critique <target> [claim-or-path]` as a thin instruction-producing command for idea, strategy, roadmap, spec, and decision-brief targets.
- Added invalid-target handling, progressive-retrieval and evidence/judgement guidance, CLI tests, and aligned README, CLI, critique-mode, and packaged-skill documentation.
- Assumption: the first CLI version should print an agent instruction, consistent with Mole's current thin command architecture, rather than perform the critique itself.
- Validation: `npm test` passed with 42 tests; supported-target and invalid-target CLI checks passed; `git diff --check` passed.
- Next priority: human test the critique workflow against a real Mole workspace, then continue with the audit foundations in issue #23 or #18.
- Independent review: `READY FOR HUMAN TESTING`. No blocking or should-fix findings. The instruction-only behavior is intentional and documented; the residual risk is that the CLI does not itself read a target file or produce a judgement.
- Human test focus: run `mole critique idea "..."` and `mole critique spec <path>`, confirm the output names the expected Mole layers, separates evidence from judgement, and points to the critique template. Confirm an unsupported target fails without a success message.

2026-09-08

- Implemented GitHub issue #23 stable source provenance: the workspace now has a versioned source registry with immutable source IDs, SHA-256 content hashes, source metadata, attachment child records, original/current paths, path history, and hash history.
- CLI and UI captures emit and register provenance fields. Existing local files and imported exports can be adopted with mole sources register/import without rewriting raw content.
- Added source-ID resolution after archive moves, correction sync with the same ID, report-only legacy path migration with exact/hash/date checks, ambiguous/unresolved statuses, and duplicate/conflict findings that preserve all records.
- Inbox receipts now retain legacy processed paths plus source_references, inbox audit recognizes source IDs, and local metrics deduplicate processed items by source ID across path changes.
- Added schema/glossary/migration documentation, evidence/context/retrieval-receipt templates, CLI guidance, focused capture/archive/attachment/correction/migration/conflict/receipt/metrics fixtures, and scaffold support for governance/source-registry.json.
- Assumptions: source IDs use UUID-backed src_ values; content hashes use SHA-256 after excluding the self-referential content_hash frontmatter line; migration --write adopts verified files into the registry but never rewrites raw files or reference text.
- Validation so far: node --test cli/test/mole.test.mjs passed with 48 tests; syntax checks and git diff --check passed.
- Next: complete independent review against issue #23 acceptance, run the full npm test command, then commit and publish the requested draft PR.

2026-09-08

- Independent bwh-agent-review completed against GitHub issue #23, the PRD task, source-of-truth templates, and the affected CLI/UI/receipt/metrics surfaces. No blocking or should-fix findings remain.
- Review fixes hardened archive resolution and receipt adoption so reused paths cannot silently claim a prior source without source-ID or content-hash evidence. Explicit mole sources sync remains the deliberate correction path.
- Final validation: npm test passed with 49 tests; all CLI, library, and UI module syntax checks passed; PRD and registry JSON parsed; git diff --check passed; CLI help exposed register/import/migrate/sync/audit.
- Residual risk: human output testing should verify a real synced-folder/archive move, a UI capture with an existing attachment, and a migration report containing both ambiguous and unresolved references before adoption.
- Human output focus: confirm raw bytes remain unchanged during import and migration, source IDs remain stable after moves/corrections, duplicate/conflict findings stay actionable, and evidence/context/receipts use source IDs rather than filenames.
- Verdict: READY FOR HUMAN TESTING. Successful human testing hands the accepted change to bwh-archive-change; additional findings return to bwh-development.
- Next: commit the completed change, push codex/issue-23, create one draft PR targeting main, and request the project review with the exact comment @codex review.

2026-09-09

- Completed the four P1 remediation fixes from automated review: non-text sources and attachments hash raw bytes, duplicate live source IDs are rejected as ambiguous before registry mutation, source-registry updates use a cross-process lock plus atomic replacement, and legacy discovery excludes scaffold guidance and directory examples by default.
- Added focused regressions for binary hash identity, duplicate live IDs, fresh scaffold migration, and concurrent source imports.
- Independent bwh-agent-review: READY FOR HUMAN TESTING. No blocking or should-fix findings remain.
- Validation: node --check passed for changed JavaScript modules; npm test passed with 53 tests; fresh scaffold discovery probe returned []; git diff --check passed.
- Human output focus: verify import/migration preserves raw bytes, duplicate-ID warnings remain actionable, concurrent captures retain every registry record, and scaffold guidance is absent from migration reports.
- Next: commit and push codex/issue-23, then request the exact @codex review comment again; do not merge.

2026-09-09

- Addressed the second automated review pass: unchanged no-ID re-registration now reuses a unique path-and-hash record; ambiguous adoption preserves a null receipt source ID with a warning; active lock owners are protected by PID liveness and ownership tokens; and CLI migration passes --include-guidance through to discovery.
- Added focused assertions for each behavior while retaining the raw-byte, duplicate-ID, serialized-write, and scaffold-safe regressions.
- Validation: npm test passed with 54 tests; changed JavaScript syntax checks passed; git diff --check passed.
- Independent review: READY FOR HUMAN TESTING. Residual human focus remains raw-byte preservation, actionable ambiguity warnings, concurrent registry integrity, and migration guidance filtering.
- Next: commit and push the second remediation checkpoint, then request the exact @codex review comment again; do not merge.
